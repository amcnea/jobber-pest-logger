# Optional Firebase (shared shop). Leave empty → local-only mode (current behavior).
# Do not commit real secrets. Create/join UI and security rules come in later PRs.
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
